import $store from "@/store/store.js";
const getters = {};
const setters = {
  setSelected(bunchId) {
    let bunchLayerList = $store.getters.bunchLayerList;
    bunchLayerList = bunchLayerList.map((item, index) => {});
  }
};
